---
title: Kmom06 - Hashtabeller
description: Kmom06 inlämningsuppgift om hashtabeller
sidebar: 
    order: 0060
    hidden: true
---

import Figure from '@components/CustomFigure.astro';

import klassdiagram from '@assets/img/kmom06/BusStopUMLkmom06.png';

I denna uppgift ska du dels implementera en hashtabell och dels utöka klassen BusStop från tidigare kursmoment. Hashtabellen kommer att användas i det avslutande projektet i kursen.


### Förberedelser

I `kmom06` skapar du ett dotnet console-projekt namngivet `HashTable`.

Kopiera klassen `BusStop` som du har i `kmom02` till katalogen `HashTable` och ändra dessutom namespace till `HashTable`. I klassdiagrammet nedan ser du vilka ändringar som ska göras i klassen `BusStop` i kmom06 jämfört med den version av `BusStop` som du hade i kmom02. 

<Figure
    src={klassdiagram}
    caption="BusStop klassdiagram"
    width="400px"
/>

Metoderna `Equals` och `GetHashCode` i klassen `BusStop` ska tillföras (dessa överskuggar de från klassen `object` precis som tidigare metoden `ToString`). `Equals` ger svar på om de båda objekten har samma tillstånd, dvs samma värde på alla instansvariabler. `GetHashCode` returnerar ett heltal som är lika för alla objekt som är lika enligt Equals. Det är viktigt att dessa två metoder är konsekventa, dvs att de uppfyller kravet att om `a.Equals(b)` så ska `a.GetHashCode() == b.GetHashCode()`. För att implementera `GetHashCode` kan du använda dig av `HashCode.Combine` som tar ett godtyckligt antal argument och returnerar en hashkod som är lika för alla kombinationer av argument som är lika.

#### Kontrollera de nya metoderna i BusStop

I Program.cs i ditt dotnet-projekt ska du skriva kod som kontrollerar att metoderna `Equals` och `GetHashCode` fungerar korrekt. Det ska tydligt framgå vilka tester som görs och vad som förväntas hända i varje test. 

### Hashtabell

Du ska i denna uppgift implementera en hashtabell i klassen `HashTable`. Klassen `HashTable` ska implementeras baserat på det generiska interfacet `IHashTable` enligt nedan. Både klassen `HashTable` och interfacet `IHashTable` ska tillhöra namespace `HashTable` och de ska vara generiska med två typparametrar, `Key` och `Value`. Interfacet `IHashTable` hittar du nedan och detta skapar du själv i filen `IHashTable.cs`.

```csharp
public interface IHashTable<Key, Value>
{
    public bool Add(Key key, Value element);
    public bool Remove(Key key);
    public bool Contains(Key key);
    public Value Get(Key key);
    public double LoadFactor();
}
```

Det är tillåtet och dessutom lämpligt att tillföra hjälpmetoder för att lösa vissa delproblem.

Hashtabellen ska använda en array och storleken/kapaciteten på denna ska vara alltid vara ett primtal.

Det är viktigt att du har en plan för hur du ska hantera kollisioner: linjär sondering eller kedjning. Du får även välja annan metod om du vill men då behöver du motivera och förklara detta val tydligt (se längre fram i texten). 

Du behöver förutom kollisioner även hantera utökning av hashtabellen om fyllnadsgraden (belastningsgraden) passerar en relevant gräns, vilken beror på hur du valt att hantera kollisioner. Även detta ska motiveras.

#### Kontrollera att din hashtabell fungerar korrekt

I `Program.cs` i ditt dotnet-projekt ska du, förutom testerna för BusStop, skriva kod som kontrollerar att hashtabellen fungerar. Du behöver tänka igenom vilka tester som behöver göras och det ska tydligt framgå vilka tester som görs och vad som förväntas hända i varje test. Utskrifter som redogör för vad som testas och vilket resultat som förväntas tillsammans med det resultat som erhålls. 

Bland annat ska objekt av klasstypen BusStop ska användas för testerna, men du bör dessutom lämpligen skapa en enkel klass som du kan använda för att testa kollisionshanteringen.  
*Tips för denna: En instansvariabel som är ett heltal vilket dessutom motsvarar resultatet av `GetHashCode()` tillsammans med att du känner till HashTabellens kapacitet.*

Avslutningsvis i `Program.cs` ska du skriva en kommentar där du motiverar ditt val av kollisionshantering och gränsen för rehashning.


#### Filer som ingår i inlämningen

I ditt dotnet-projekt ska följande filer ingå
- `BusStop.cs` - som innehåller klassen BusStop
- `IHashTable.cs` - som innehåller interfacet IHashTable
- `HashTable.cs` - som innehåller klassen HashTable som implementerar IHashTable
- `Program.cs`- där du har tester för BusStop och HashTable samt din motivering av val av kollisionshantering.

### Detta förväntas du kunna efter att du genomfört uppgiften

- Förstå och kunna skapa klasser i vilka du definierar hur hashkoden ska bestämmas
- Implementera metoder som representerar operationer för en hashtabell
- Hantera kollisioner i en hashtabell
